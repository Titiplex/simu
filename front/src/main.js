import { createApp } from 'vue';       // Importer la fonction createApp de Vue
import App from './App.vue';           // Importer ton composant principal (App.vue)

createApp(App).mount('#app');         // Créer l'application Vue et la monter sur l'élément avec l'id "app"
